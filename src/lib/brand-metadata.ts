/**
 * Values that must match the primary Jersey brand token in `tailwind.config.ts`
 * (`jersey-red` DEFAULT) and `--color-jersey-red` in `globals.css`. Used where
 * the platform APIs (viewport, metadata) cannot read CSS variables.
 */
export const JERSEY_RED_PRIMARY_HEX = "#A31621";
